import React from 'react'

const Button = ({label}) => {
  return (
    <button className='h-[50px] w-full rounded-[12px] bg-[#FF6B57] text-white text-[15px] font-inter font-bold '>{label}</button>
  )
}

export default Button